// public/ai-bridge.js
console.log("🚀 AI bridge script loaded");

// Contoh fungsionalitas dummy: bisa panggil alert atau log
window.initAIBridge = function () {
  console.log("AI Bridge initialized");
  // Misal, bisa hubungkan ke API AI atau lakukan setup lainnya
};

// Jalankan otomatis saat script dimuat
window.initAIBridge();
